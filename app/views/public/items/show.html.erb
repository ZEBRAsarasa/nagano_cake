<div class="col-md-3">
  <table>
    <thead>
      <tr>
        <th>ジャンル検索</th>
      </tr>
    </thead>
    <tbody>
        <% @genres.each do |genre| %>
          <tr>
            <td><%= genre.name %></td>
          </tr>
    </tbody>
    <% end %>
  </table>
</div>
<div class="col-md-8 offset-md-1">
  <%= attachment_image_tag @item, :image, :fill, 200, 200 %>
    <h3><%= @item.name %></h3>
    <p><%= @item.introduction %></p>
    <p>¥<%= (@item.price_excluding_tax*1.1).round.to_s(:delimited) %>(税込)</p>
    <%= form_with model: @item, local:true do |f| %>
    <p><%=  %><%=  %></p>
    <% end %>
</div>